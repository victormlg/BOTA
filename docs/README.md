# Language

This Language is a functional statically typed shell and configuration language. 
Its goals are: minimal syntax, functional (like haskell), glue (like bash), configuration (like nix), type safety (like Rust).
It is designed to be the sole configuration and shell language of a custom OS


## Documentation

1. basics
2. types
3. functions
4. configuration
