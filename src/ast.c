#include "ast.h"
#include "bota.h"
/*
NodeRef TypeNew()
{

}

NodeRef BinaryOperationNodeNew(BOTAContext *ctx ,NodeRef left, BinaryOperationType operator, NodeRef right)
{
  NodeRef node_idx = BOTAContextAllocate(ctx, sizeof(BinaryOperationNode));
  BinaryOperationNode *node = (BinaryOperationNode *) &ctx->ast_pool[node_idx.ptr];

  node->left = left;
  node->right = right;
  node->type = operator;

  return node_idx;
}
*/


